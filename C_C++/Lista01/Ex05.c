#include <stdio.h>
#include <math.h>

int main(){

    float n1;
    printf("Digite um numero real (float): ");
    scanf("%f", &n1);
    printf("\na quinta parte desse numero = %.3f", (n1/5));
    return 0;
}